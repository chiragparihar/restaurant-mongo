author@chirag parihar
studentno@101130054

To install all the node modules run:
npm install

first run the mongod daemon and then run the server

To run the server:
node server.js 

database intializer file is untouched and the same as was given, the public folder was disintegrated and was files were moved to restaurant data, other than that everything should be good

